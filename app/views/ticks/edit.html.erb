<% if @tick.route_id.present? %>

<%= render(
partial: 'routes/route',
locals: { route: @route }
) %>

<%= render(
partial: 'ticks/route_tick_form',
locals: { tick: @tick }
) %>

<% else %>

<%= render(
partial: 'ticks/form',
locals: { tick: @tick }
) %>

<% end %>
