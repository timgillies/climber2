$('body').scrollTop(0);
$('#grade_render').html('');
$('#wall_render').html('');
$('#tickForm')[0].reset();





var $successMessageDiv = $('#successMessage'); // get the reference of the div
$successMessageDiv.show().html('Tick saved!'); // show and set the message
