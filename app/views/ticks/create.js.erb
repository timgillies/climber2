$('body').scrollTop(0);
$('#grade_render').html('');
$('#wall_render').html('');
$('#tickForm')[0].reset();
$("#tick-stats").html("<%= j (render(partial: 'users/user_stats_tick_count')) %>");
$("#hardest-send").html("<%= j (render(partial: 'users/user_stats_hardest_send')) %>");






var $successMessageDiv = $('#successMessage'); // get the reference of the div
$successMessageDiv.slideDown(100,'linear').html('Tick saved!'); // show and set the message
