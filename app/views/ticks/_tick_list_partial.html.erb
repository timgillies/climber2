<div>
  <div class="row">
    <div class="routelist-header bg-blue-gradient">
      <h1 class="hidden-xs">Ascents</h1>
      <h1 class="hidden-sm hidden-md hidden-lg hidden-xl"><strong>tick</strong>list </h1>
    <span class="pull-right">
      <span><%= link_to "daily summary", user_ticks_path(current_user), class: "btn btn-close", style: "display: inline-block;" %></span>
    </span>
  </div>
  </div>
</div>

<br>

<%= form_for_filterrific @filterrific do |f| %>




  <div class="row">
      <div class="route-form">
        <div id="donate">
          <% tick_date_filters.each do |zone| %>
            <label class="green"><%= f.radio_button :with_date_gte, zone[1] %>
              <span><%= zone[0] %></span>
            </label>
          <% end %>
        </div>
      </div>
  </div>

<% end %>


<div class="row">
  <%= render(
  partial: 'ticks/tick_list_list',
  locals: { ticks: @ticks }
  ) %>
</div>
