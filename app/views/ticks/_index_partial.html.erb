<div>
  <div class="row">
    <div class="routelist-header bg-blue-gradient">
      <h1 class="hidden-xs">daily summary</h1>
      <h1 class="hidden-sm hidden-md hidden-lg hidden-xl"><strong>Daily</strong>summary</h1>
    <span class="pull-right">
      <span><%= link_to "see all", tick_list_user_ticks_path(@user), class: "btn btn-close", style: "display: inline-block;" %></span>
    </span>
  </div>
  </div>
</div>

<br>



<div class="row">

  <%= render(
  partial: 'ticks/user_ticks',
  locals: { ticks: @ticks }
  ) %>

</div>

<div class="row">

  <%= render(
  partial: 'ticks/usergradedist',
  locals: { ticks: @ticks }
  ) %>

</div>


<br>

<%= form_for_filterrific @filterrific do |f| %>




  <div class="row">
      <div class="route-form">
        <div id="donate">
          <% tick_date_filters.each do |zone| %>
            <label class="green"><%= f.radio_button :with_date_gte, zone[1] %>
              <span><%= zone[0] %></span>
            </label>
          <% end %>
        </div>
      </div>
  </div>

<% end %>


<div class="row">
  <%= render(
  partial: 'ticks/daily_summary',
  locals: { ticks: @ticks }
  ) %>
</div>
