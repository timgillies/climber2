<h1><small> Route by Setter </small></h1>
<script type="text/javascript" charset="utf-8">
  $(function() {
    new Highcharts.Chart({
      chart: {
        renderTo: "wall_dist",
        type: "bar"
      },
      title: {
        text: ""
      },
      xAxis: {
        type: 'category',
        categories: <%= raw @facility.walls.all.order('name ASC').map {|f| [f.name] } %>,
        labels: {
          enabled: true
        },
        tickLength: 0,
        lineWidth: 0,
        minorGridLineWidth: 0,
        minorTickLength: 0,
        lineColor: 'transparent'
      },
      yAxis: {
        title: {
          text: ""
        },
        labels: {
          enabled: false
        },
        tickLength: 0,
        lineWidth: 0,
        minorGridLineWidth: 0,
        gridLineColor: 'transparent',
        minorTickLength: 0,
        lineColor: 'transparent',
        allowDecimals: false
      },
      credits: {
        enabled: false
      },
      legend: {
        enabled: false
      },
      plotOptions: {
        bar: {
          dataLabels: {
            enabled: true,
            style: {
              textShadow: '0 0 0 black'
            }
          }
        }
      },
      colors: (["#6bda8f", "#b6da6b", "#da8f6b", "#da6bb6"]),

      series: [{
          type: 'bar',
          name: 'Routes',
          data: <%= @facility.walls.all.map {|f| [ @activeroutes.where("wall_id = ?", f).count] } %>
      }]
    });
  });
</script>
<div id="wall_dist" style="width:100%; height:200px;"></div>
