<% provide(:title, @route.name) %>

<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= @route.name %>
      </h1>

      <h2>
        <%= @route.color  %>
        <%= @route.setter %>
        <%= @route.created_at %>
      </h2>
    </section>
  </aside>
</div>
