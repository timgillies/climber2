<div class="container-fluid">
  <div class="row">
    <div class="routelist-header bg-blue-gradient">
      <h1><strong>Route</strong>info </h1>
    </div>
  </div>
</div>

<br>

<%= render(
partial: 'routes/route',
locals: { route: @route }
) %>

<%= render(
partial: 'ticks/route_tick_form',
locals: { tick: @tick }
) %>
