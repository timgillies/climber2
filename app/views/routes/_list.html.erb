<div id="filterrific_results">
  <div class="row">
    <div class="col-xs-12">
      <div class="routelist">
      <h2><%= page_entries_info @routes  %>
      <% if filter_results_count > 0 %>
        (<%= filter_results_count.to_s + ' filter'.pluralize(filter_results_count) + ' applied' %>)
      <% end %>
      </h2>
    </div>
      <br>
      <br>
    </div>

  </div>

  <div id="routes-list">
      <%= render(
      partial: 'routes/listitem') %>
  </div>

<%= paginate(@routes) %>
</div>
