<ul class="grades">
  <% @grades.each do |grade| %>
    <li>
      <%= link_to grade.grade,  edit_facility_grade_path(@facility, grade) %>
      <%= grade.discipline  %>
      value: <%= grade.rank %>
    </li>
  <% end %>
</ul>
