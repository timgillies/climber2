

  <div class="col-md-4 col-md-offset-4 col-sm-6 col-sm-offset-3 col-lg-4 col-lg-offset-4">
    <div class="span7 text-center">
      <div class="row">
        <% if @userfacilities.count > 1 %>
        <div class="btn-group btn-homeaction btn-block">
          <button type="button" class="btn btn-homeaction btn-lg btn-block dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Add routes <span class="caret"></span>
          </button>
          <ul class="dropdown-menu">
            <% @userfacilities.each do |f| %>
            <li><%= link_to f.name + " " + f.location, new_admin_facility_route_path(f) %></li>
            <% end %>
          </ul>
        </div>
        <br/>
        <br/>
        <div class="btn-group btn-homeaction  btn-block">
          <button type="button" class="btn btn-homeaction btn-lg btn-block dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Find routes <span class="caret"></span>
          </button>
          <ul class="dropdown-menu">
            <% @userfacilities.each do |f| %>
            <li><%= link_to f.name + " " + f.location, admin_facility_routes_path(f) %></li>
            <% end %>
          </ul>
        </div>

        <% elsif @userfacilities.count == 1 %>
          <%= link_to "ADD ROUTES", new_admin_facility_route_path(@userfacilities.first), class: "btn btn-homeaction btn-lg btn-block" %>
          <%= link_to "FIND ROUTES", admin_facility_routes_path(@userfacilities.first), class: "btn btn-homeaction btn-lg btn-block" %>
        <% else %>
          <%= link_to "Register your facility", new_admin_facility_path, class: "btn btn-signup btn-lg" %>
        <% end %>

        </div>
    </div>
  </div>
