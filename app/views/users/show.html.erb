<% provide(:title, current_user.name ) %>

<div class="container-fluid">
  <div class="row">
    <div class="col-md-12">
      <div class="headliner-line">
        <h1> <%= @user.name %></h1>
          <%= render 'users/profile', object: @user %>
      </div>
    </div>
  </div>
</div>
