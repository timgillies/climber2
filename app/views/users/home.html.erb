<% provide(:title, "Home") %>

<div class="container-fluid">
  <div class="row">
    <div class="col-sm-12">
      <div class="row">
        <div class="gray-labels routeform-header bg-blue-gradient">
          <h1> NEWS FEED</h1>
        </div>
      </div>
      <div class="row">
        <div class="col-sm-4">
          <%= render 'users/top10sends', object: @user_tick_feed %>
          <%= render 'users/top10routes', object: @user_tick_feed %>
          <%= render 'users/newest10routes', object: @user_tick_feed %>
        </div>
        <div class="col-sm-8">
            <%= render 'users/user_tick_feed', object: @user_tick_feed %>
        </div>
      </div>
    </div>
  </div>
</div>
