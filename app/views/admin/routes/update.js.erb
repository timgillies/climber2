$('#routeForm')[0].reset();
$('html, body').animate({
    scrollTop: $("#bodyTop").offset().top
}, 250);
$('#grade_render').html('');
$('#wall_render').html('');
$("#routeShow").html("<%= escape_javascript(render(partial: 'routes/route', locals: { f: @route })) %>");


var $successMessageDiv = $('#successMessage'); // get the reference of the div
$successMessageDiv.show().html("<i class='fa fa-check-circle'></i> Route updated!"); // show and set the message
