<ul class="grades">
  <% @facility.grades.each do |grade| %>
    <li>
      <%= grade.discipline.upcase  %> -
      <%= grade.grade %> -
      <%= grade.system %> -
      value: <%= grade.rank %>
      <%= link_to "edit",  edit_admin_facility_grade_path(@facility, grade) %>
    </li>
  <% end %>
</ul>
