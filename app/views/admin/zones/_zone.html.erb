
  <%= div_for @zones do |f| %>
  <ol class="routelist">
    <li>
      <div class="container-fluid">
        <div class="row">
          <div class="col-sm-5 col-xs-4">
            <p><%= f.name %></p>
            </h3>
          </div>
          <div class="col-sm-5 col-xs-4">
              <p><%= f.routes.current.count %> active routes</p>
          </div>
          <div class="col-xs-2 col-sm-1 col-md-1">
            <p><%= link_to edit_admin_facility_zone_path(@facility, f ) do %>
                        <i class="glyphicon glyphicon-cog"></i>
            </p>            <% end %>
          </div>

          <div class="col-xs-2 col-sm-1 col-md-1">
            <p><%= link_to admin_facility_zone_path(@facility, f), data: {:confirm => 'Are you sure? This cannot be undone.'},
                        :method => :delete do %>
                        <i class="glyphicon glyphicon-trash"></i>
            </p>            <% end %>
          </div>
        </div>
        <% f.walls.each do |w| %>
        <div class="row">
          <div class="col-sm-5 col-xs-4">
            <p class="tab"><%= w.name %></p>
          </div>
          <div class="col-sm-5 col-xs-4">
            <p><%= w.routes.current.count %></p>
          </div>
          <div class="col-xs-2 col-sm-1 col-md-1">
            <p><%= link_to edit_admin_facility_wall_path(@facility, w ) do %>
                        <i class="glyphicon glyphicon-cog"></i>
            </p>            <% end %>
          </div>

          <div class="col-xs-2 col-sm-1 col-md-1">
            <p><%= link_to admin_facility_wall_path(@facility, w), data: {:confirm => 'Are you sure? This cannot be undone.'},
                        :method => :delete do %>
                        <i class="glyphicon glyphicon-trash"></i>
            </p>            <% end %>
          </div>
        </div>
        <div class="row">
          <% w.sub_child_zones.each do |s| %>
            <div class="col-sm-5 col-xs-4">
              <p class="doubletab"><%= s.name %></p>
            </div>
            <div class="col-sm-5 col-xs-4">
              <p><%= s.routes.current.count %></p>
            </div>
            <div class="col-xs-2 col-sm-1 col-md-1">
              <p><%= link_to edit_admin_facility_sub_child_zone_path(@facility, s ) do %>
                          <i class="glyphicon glyphicon-cog"></i>
              </p>            <% end %>
            </div>

            <div class="col-xs-2 col-sm-1 col-md-1">
              <p><%= link_to admin_facility_sub_child_zone_path(@facility, s), data: {:confirm => 'Are you sure? This cannot be undone.'},
                          :method => :delete do %>
                          <i class="glyphicon glyphicon-trash"></i>
              </p>            <% end %>
            </div>
          <% end %>
        </div>
        <% end %>
      </div>
    </li>
  </ol>
  <% end %>
