<%= form_for [:admin, @facility, @zone] do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <%= f.text_field :name, class: 'form-control', placeholder: "Add a zone..." %>


  <%= f.submit "Submit", class: "btn btn-submit" %>

<% end %>
