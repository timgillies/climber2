<% provide(:title, 'Zones') %>

<div class="container-fluid">
  <div class="row">
    <div class="col-sm-6">
      <div class="routes">
        <h1><small>Edit zone</small></h1>
      </div>
      <%= render 'admin/zones/form', object: @zones %>
    </div>
    <div class="col-sm-4">
      <div class="routes">
        <h1><small>Zones</small></h1>
      </div>
      <%= render 'admin/zones/zone', object: @zones %>
    </div>
  </div>
